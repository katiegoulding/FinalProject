library(shiny)
library(dplyr)
library(ggplot2)
library(plotly)
library(tidyr)


speed.data <- read.csv("data/simpleSpeedDating.df.csv")


server <- function(input, output) {
  
  
}
shinyServer(server)